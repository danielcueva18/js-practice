// Challenge
// Add value to the end of the array
// turn 'Giants' into a different team
// access the 'Dodgers' value and save it to variable myFavoriteTeam
// access the length of the array

var baseballTeam = ['Dodgers', 'Giants', 'Mets', 'Yankees', 'Astros'];

console.log(baseballTeam.length)

baseballTeam[5] = 'Cardinals'
console.log(baseballTeam)

baseballTeam[1] = 'Red Soxs'
console.log(baseballTeam)

var myFavoriteTeam = baseballTeam[0]
console.log(myFavoriteTeam)

console.log(baseballTeam.length)